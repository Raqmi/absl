<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- pos session form view -->
    <record id="session_inherit" model="ir.ui.view">
        <field name="name">session.inherit</field>
        <field name="model">pos.session</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_session_form" />
        <field name="arch" type="xml">
            <field name="config_id" position="after">
                <field name="ticket_num" />
                <field name="first_ticket" />
                <field name="last_ticket" />
                <field name="total_disc" />
            </field>
        </field>
    </record>

    <!-- account bank statement line form view -->
    <record id="cash_in_out_inherit" model="ir.ui.view">
        <field name="name">cash.in.out.inherit</field>
        <field name="model">account.bank.statement.line</field>
        <field name="inherit_id" ref="account.view_bank_statement_line_form" />
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="cash_in_out" />
            </field>
        </field>
    </record>
</odoo>
